/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow fetching from backend during local dev and when API_URL is set on Vercel
  reactStrictMode: true,
};

module.exports = nextConfig;
